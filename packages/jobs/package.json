{
  "name": "@app/jobs",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --clean --dts --format esm,cjs --out-dir dist",
    "lint": "biome lint ./src",
    "lint:fix": "biome lint --write ./src",
    "check:type": "tsc --project tsconfig.json --noEmit",
    "test": "vitest",
    "sync": "tsx src/cli/run.ts"
  },
  "dependencies": {
    "axios": "^1.7.2",
    "zod": "^3.22.2"
  },
  "devDependencies": {
    "@types/node": "^22.7.8",
    "nock": "^13.5.2",
    "tsup": "^8.2.4",
    "tsx": "^4.19.2",
    "typescript": "^5.9.2",
    "vitest": "^2.1.1"
  }
}
