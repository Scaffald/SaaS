{
  "name": "scaffald",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "env-local": "dotenv -e .env --",
    "env-prod": "dotenv -e .env.production --",
    "dev": "pnpm --filter expo-app start",
    "ios": "pnpm --filter expo-app ios",
    "android": "pnpm --filter expo-app android",
    "web": "pnpm --filter expo-app web",
    "web:build": "pnpm --filter expo-app web:build",
    "prebuild": "pnpm --filter expo-app prebuild",
    "deploy": "./scripts/deploy.sh",
    "deploy:preview": "./scripts/deploy.sh --preview",
    "deploy:prod": "./scripts/deploy.sh --production",
    "deploy:quick": "./scripts/deploy.sh --skip-build",
    "watch": "turbo watch",
    "format": "turbo format",
    "format:fix": "turbo format:fix",
    "postinstall": "pnpm check-deps",
    "build": "turbo build",
    "check": "turbo check",
    "check-deps": "check-dependency-version-consistency .",
    "check-circular-deps": "turbo check-circular-deps",
    "ui:check": "pnpm --filter @app/ui check",
    "ui:optimize": "pnpm --filter @app/ui optimize",
    "ui:extract": "pnpm --filter @app/ui extract",
    "tamagui:check": "tamagui check",
    "tamagui:upgrade": "pnpm up '*tamagui*'@latest '@tamagui/*'@latest",
    "tamagui:upgrade:canary": "pnpm up '*tamagui*'@canary '@tamagui/*'@canary",
    "supa": "pnpm with-env pnpx supabase --workdir packages",
    "supa:seed": "pnpm with-env pnpm --filter @app/supabase seed",
    "supa:seed:prod": "pnpm env-prod pnpm --filter @app/supabase seed",
    "supa:push": "pnpm with-env pnpx supabase --workdir packages db push",
    "supa:push:seed": "pnpm with-env pnpx supabase --workdir packages db push --include-seed",
    "supa:push:prod": "pnpm env-prod pnpx supabase --workdir packages db push",
    "supa:push:seed:prod": "pnpm env-prod pnpx supabase --workdir packages db push --include-seed",
    "supa:functions": "pnpm with-env pnpx supabase --workdir packages functions serve --env-file ./.env",
    "supa:generate": "pnpm supa gen types typescript --local > packages/supabase/types.ts",
    "supa:generate:remote": "pnpm with-env pnpx supabase gen types typescript > packages/supabase/types.ts",
    "supa:generate:prod": "pnpm env-prod pnpx supabase gen types typescript > packages/supabase/types.ts",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "generate": "turbo gen",
    "test:build": "pnpm build && echo '✅ All packages built successfully'",
    "test:lint": "pnpm lint:fix && pnpm typecheck && pnpm check-deps",
    "test:deps": "pnpm check-deps && pnpm check-circular-deps",
    "test:tamagui": "pnpm tamagui:check",
    "test:integration": "pnpm test:deps && pnpm test:lint && pnpm test:tamagui",
    "test:all": "pnpm test:integration && pnpm test:build",
    "test:performance": "cd apps/expo && time pnpm web:build",
    "validate:migration": "pnpm test:all && echo '✅ Migration validation complete'"
  },
  "packageManager": "pnpm@10.17.1",
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@tamagui/cli": "catalog:",
    "@tamagui/config": "catalog:",
    "@turbo/gen": "^2.5.8",
    "check-dependency-version-consistency": "^3.3.0",
    "fs-extra": "^11.2.0",
    "netlify-cli": "^23.9.1",
    "turbo": "catalog:",
    "typescript": "catalog:"
  },
  "dependencies": {
    "date-fns": "^4.1.0"
  }
}
