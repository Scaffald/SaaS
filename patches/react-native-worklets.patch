diff --git a/Common/cpp/worklets/Resources/ValueUnpacker.cpp b/Common/cpp/worklets/Resources/ValueUnpacker.cpp
--- a/Common/cpp/worklets/Resources/ValueUnpacker.cpp
+++ b/Common/cpp/worklets/Resources/ValueUnpacker.cpp
@@ -40,7 +40,9 @@ const char ValueUnpackerCode[] =
     } else if (category === 'RemoteFunction') {
       var fun = function fun() {
         var label = remoteFunctionName ? "function `".concat(remoteFunctionName, "`") : 'anonymous function';
-        throw new Error("[Worklets] Tried to synchronously call a non-worklet ".concat(label, " on the UI thread.\nSee https://docs.swmansion.com/react-native-worklets/docs/guides/troubleshooting#tried-to-synchronously-call-a-non-worklet-function-on-the-ui-thread for more details."));
+        // Downgraded from throw to console.warn for Reanimated 4.2.1 + drawer-layout compat.
+        // addListener/removeListener are called from mappers; they're safe no-ops here since
+        // mappers still run via dirty-flag polling on animation frames.
+        console.warn("[Worklets] Tried to synchronously call a non-worklet ".concat(label, " on the UI thread."));
       };
diff --git a/lib/module/memory/serializable.native.js b/lib/module/memory/serializable.native.js
--- a/lib/module/memory/serializable.native.js
+++ b/lib/module/memory/serializable.native.js
@@ -521,6 +521,16 @@ function freezeObjectInDev(value) {
   if (!__DEV__ || globalThis.__RUNTIME_KIND !== RuntimeKind.ReactNative) {
     return;
   }
+  // Reanimated 4 shared values register themselves in serializableMappingCache before
+  // any worklets run. On a cache miss (e.g. fast-refresh clears the cache), createSerializable
+  // falls back to clonePlainJSObject which calls this function. Freezing a shared value
+  // replaces its `.value` setter with a no-op, silently breaking animations
+  // (e.g. the expo-router/drawer drawer never opening on iOS). Skip them instead.
+  if (value != null && value._isReanimatedSharedValue === true) {
+    return;
+  }
   Object.entries(value).forEach(([key, element]) => {
diff --git a/lib/module/memory/bundleUnpacker.native.js b/lib/module/memory/bundleUnpacker.native.js
--- a/lib/module/memory/bundleUnpacker.native.js
+++ b/lib/module/memory/bundleUnpacker.native.js
@@ -15,10 +15,10 @@
     }
     return value;
   } else if (category === 'RemoteFunction') {
-    const remoteFunctionHolder = () => {
+    const remoteFunctionHolder = (...args) => {
       const label = remoteFunctionName ? `function \`${remoteFunctionName}\`` : 'anonymous function';
-      throw new WorkletsError(`Tried to synchronously call a non-worklet ${label} on the UI thread.
-See https://docs.swmansion.com/react-native-worklets/docs/guides/troubleshooting#tried-to-synchronously-call-a-non-worklet-function-on-the-ui-thread for more details.`);
+      // Downgraded from throw to console.warn for Reanimated 4.2.1 + drawer-layout compat.
+      console.warn(`[Worklets] Tried to synchronously call a non-worklet ${label} on the UI thread.`);
     };
     remoteFunctionHolder.__remoteFunction = objectToUnpack;
     return remoteFunctionHolder;
diff --git a/lib/module/memory/valueUnpacker.native.js b/lib/module/memory/valueUnpacker.native.js
--- a/lib/module/memory/valueUnpacker.native.js
+++ b/lib/module/memory/valueUnpacker.native.js
@@ -42,11 +42,12 @@
       }
       return value;
     } else if (category === 'RemoteFunction') {
-      const fun = () => {
+      const fun = (...args) => {
         const label = remoteFunctionName ? `function \`${remoteFunctionName}\`` : 'anonymous function';
-        // eslint-disable-next-line reanimated/use-worklets-error
-        throw new Error(`[Worklets] Tried to synchronously call a non-worklet ${label} on the UI thread.
-See https://docs.swmansion.com/react-native-worklets/docs/guides/troubleshooting#tried-to-synchronously-call-a-non-worklet-function-on-the-ui-thread for more details.`);
+        // Downgraded from throw to console.warn for Reanimated 4.2.1 + drawer-layout compat.
+        // The addListener/removeListener calls from mappers are safe no-ops when they can't
+        // register — the mapper will still run via dirty-flag polling on animation frames.
+        console.warn(`[Worklets] Tried to synchronously call a non-worklet ${label} on the UI thread.`);
       };
       fun.__remoteFunction = objectToUnpack;
       return fun;
