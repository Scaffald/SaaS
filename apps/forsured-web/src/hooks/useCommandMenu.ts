/**
 * useCommandMenu hook
 * Convenience hook for accessing CommandMenu context
 * Re-exports the hook from context for easier imports
 */

export { useCommandMenu } from '../contexts/CommandMenuContext'
