import { DashboardLayout } from '@app/ui'
import { {{ pascalCase parentName }}{{ pascalCase childName }}Screen } from '@app/core/features/{{ dashCase parentName
}}/{{ dashCase parentName }}-{{ dashCase childName }}-screen'
import Head from 'next/head'

import type { NextPageWithLayout } from '../_app'

export const Page: NextPageWithLayout = () => {
return (
<>

  <Head>
    <title>{{ titleCase parentName }} {{ titleCase childName }}</title>
  </Head>
  <{{ pascalCase parentName }}{{ pascalCase childName }}Screen />
</>
)
}

Page.getLayout = (page) => (
<DashboardLayout header={{ title: '{{ titleCase parentName }} {{ titleCase childName }}' }} rightContent={page} />
)

export default Page