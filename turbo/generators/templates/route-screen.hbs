import { FullscreenSpinner, View } from '@app/ui'

import { useUser } from '@app/core/utils/useUser'
import { {{ pascalCase parentName }}{{ pascalCase childName }}Left } from './{{ dashCase parentName }}-{{ dashCase
childName }}-left'
import { {{ pascalCase parentName }}{{ pascalCase childName }}Right } from './{{ dashCase parentName }}-{{ dashCase
childName }}-right'

export function {{ pascalCase parentName }}{{ pascalCase childName }}Screen() {
const { isPending } = useUser()

if (isPending) {
return (
<View flex={1} height="80vh" ai="center" jc="center">
  <FullscreenSpinner />
</View>
)
}

return (
<>
  <{{ pascalCase parentName }}{{ pascalCase childName }}Left />
  <{{ pascalCase parentName }}{{ pascalCase childName }}Right />
</>
)
}